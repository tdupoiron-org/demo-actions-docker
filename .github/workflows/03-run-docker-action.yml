name: 03 - Run Docker Action
run-name: Run Docker Action

on:
  workflow_dispatch:

jobs:

  run-docker-action:
    runs-on: ubuntu-latest
    steps:

      - name: Run Docker Action
        uses: docker://ghcr.io/tdupoiron-org/tdupoiron-org/demo-actions-docker:1709629510
        with:
          args: "tom"